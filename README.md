## Practice React Calculator App

### Run the following command to install node modules

`npm install`

### Run the following command to run the app after you've installed the node modules

`npm start`
